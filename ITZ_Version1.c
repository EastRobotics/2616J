#pragma config(Sensor, dgtl1,  liftLeft,       sensorQuadEncoder)
#pragma config(Sensor, dgtl3,  liftRight,      sensorQuadEncoder)
#pragma config(Motor,  port1,           claw1,         tmotorVex393_HBridge, openLoop)
#pragma config(Motor,  port2,           left_Front,    tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port3,           right_Front,   tmotorVex393HighSpeed_MC29, openLoop, reversed)
#pragma config(Motor,  port4,           arm_RightUp,   tmotorVex393HighSpeed_MC29, openLoop, reversed)
#pragma config(Motor,  port5,           arm_LeftUp,    tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port6,           arm_LeftDown,  tmotorVex393HighSpeed_MC29, openLoop, reversed)
#pragma config(Motor,  port7,           arm_RightDown, tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port8,           left_Back,     tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port9,           right_Back,    tmotorVex393HighSpeed_MC29, openLoop, reversed)
#pragma config(Motor,  port10,          claw2,         tmotorVex393_HBridge, openLoop, reversed)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

/*---------------------------------------------------------------------------*/
/*                                                                           */
/*        Description: Competition template for VEX EDR                      */
/*                                                                           */
/*---------------------------------------------------------------------------*/

// This code is for the VEX cortex platform
#pragma platform(VEX2)

// Select Download method as "competition"
#pragma competitionControl(Competition)

//Main competition background code...do not modify!
#include "Vex_Competition_Includes.c"
#define drivethreshhold 10
#define drivelimit 80
/*---------------------------------------------------------------------------*/
/*                          Pre-Autonomous Functions                         */
/*                                                                           */
/*  You may want to perform some actions before the competition starts.      */
/*  Do them in the following function.  You must return from this function   */
/*  or the autonomous and usercontrol tasks will not be started.  This       */
/*  function is only called once after the cortex has been powered on and    */
/*  not every time that the robot is disabled.                               */
/*---------------------------------------------------------------------------*/



/*-----------------------------------------------------------------------------*/
/*                                                                             */
/*  main task                                                                  */
/*                                                                             */
/*-----------------------------------------------------------------------------*/

void stack (int coneNumber){
	
}



void drive_Forward(int power) {
	motor[left_Front]= power;
	motor[left_Back] =power;
	motor[right_Front] =power;
	motor[right_Back] =power;
}

void turn_Left (int power) {
	motor[left_Front]= power;
	motor[Left_Back] =power;
	motor[Right_Front] =-power;
	motor[Right_Back] =-power;
}
void turn_Right (int power) {
	motor[left_Front]= -power;
	motor[left_Back] = -power;
	motor[right_Front] =power;
	motor[right_Back] =power;
}
void drive_Backward (int power) {
	motor[left_Front]= -power;
	motor[left_Back] = -power;
	motor[right_Front] = -power;
	motor[right_Back] = -power;
}


void arm_Up (int power) {
	motor[arm_LeftUp] =power;
  motor[arm_LeftDown] =power;
  motor[arm_RightUp] =power;
  motor[arm_RightDown] =power;
}
void arm_Down (int power) {
	motor[arm_LeftUp] =-power;
  motor[arm_LeftDown] =-power;
  motor[arm_RightUp] =-power;
  motor[arm_RightDown] =-power;

}
void claw_Open (int power) {
	motor[claw1] =power;
	motor[claw2] =power;
}
void claw_Close (int power) {
	motor[claw2] =-power;
	motor[claw1] =-power;

}

void pre_auton()
{

  // running between Autonomous and Driver controlled modes. You will need to
  // manage all user created tasks if set to false.
  bStopTasksBetweenModes = true;

	// Set bDisplayCompetitionStatusOnLcd to false if you don't want the LCD
	// used by the competition include file, for example, you might want
	// to display your team name on the LCD in this function.
	// bDisplayCompetitionStatusOnLcd = false;

  // All activities that occur before the competition starts
  // Example: clearing encoders, setting servo positions, ...
}

/*---------------------------------------------------------------------------*/
/*                                                                           */
/*                              Autonomous Task                              */
/*                                                                           */
/*  This task is used to control your robot during the autonomous phase of   */
/*  a VEX Competition.                                                       */
/*                                                                           */
/*  You must modify the code to add your own robot specific commands here.   */
/*---------------------------------------------------------------------------*/

task autonomous()
{
	claw_Close(127);
	wait10Msec(100);
	claw_Close(80);
	arm_Down(127);
	wait10Msec(150);
	arm_Down(0);
	drive_Forward(127);
	wait10Msec(150);
	drive_Forward(0);
	arm_Up(127);
	wait10Msec(100);
	arm_Up(0);
	claw_Open(127);
	wait10Msec(250);
	claw_Open(0);


  // ..........................................................................
  // Insert user code here.
  // ..........................................................................

  // Remove this function call once you have "real" code.
  AutonomousCodePlaceholderForTesting();
}

/*---------------------------------------------------------------------------*/
/*                                                                           */
/*                              User Control Task                            */
/*                                                                           */
/*  This task is used to control your robot during the user control phase of */
/*  a VEX Competition.                                                       */
/*                                                                           */
/*  You must modify the code to add your own robot specific commands here.   */
/*---------------------------------------------------------------------------*/

task usercontrol()
{
	while (true){
  if(vexRT[Btn5U]) //  Lifting Setup (I needs to figure out this PID stuff)
			arm_Up(127);
		else if(vexRT[Btn5D])
			arm_Down(127);
		else
			arm_Up(0);

	if(vexRT[Btn6U]) {   //Claw stuffs
		claw_Close(127);
 } else if(vexRT[Btn6D]) {
  	claw_Open(127);
	} else {
   claw_Close(0);
	}




	motor[right_Front] = motor[right_Back] = (abs(vexRT[Ch2])> drivethreshhold)?((vexRT[Ch2]>drivelimit)?drivelimit:((vexRT[Ch2]<(-drivelimit))?-drivelimit:vexRT[Ch2])):0;  //Driving Stuffs I need to figure out (is that a ternary inside of a ternary?)
	motor[left_Front] = motor[left_Back] = (abs(vexRT[Ch3])> drivethreshhold)?((vexRT[Ch3]>drivelimit)?drivelimit:((vexRT[Ch3]<(-drivelimit))?-drivelimit:vexRT[Ch3])):0;


if(vexRT(Btn8D)){

	arm_Up(127);
	wait10Msec(30)
	arm_Up(0);
	claw_Open(127);
	wait10Msec(30);
	claw_Open(0);
	}
while(vexRT(Btn7D)){
	arm_Up(127)
	wait10Msec(5)
	arm_Up(0)
	claw_Close(80);

	arm_Down(127);

}


}
}
